import { SectionContainerStyled } from "./SectionContainer.styled";

const SectionContainer = ({ children }) => {
  return <SectionContainerStyled>{children}</SectionContainerStyled>;
};

export default SectionContainer;
